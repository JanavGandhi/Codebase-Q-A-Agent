# .gitignore  ← protect secrets and junk
.env
.venv/
__pycache__/
*.pyc
chroma_db/        # ChromaDB will create this folder — don't commit it
repos/            # Where we'll clone the requests library